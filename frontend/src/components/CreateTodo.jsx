const CreateTodo = () => {
    return (
        <div>
            <input type="text" placeholder="Enter title" />
            <input type="text" placeholder="Enter title" />
            <button>Add a Note</button>
        </div>
    );
};

export default CreateTodo;
